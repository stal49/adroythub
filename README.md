Commands to run the project
for client - 1. 'npm run dev' (i used this for localhost and editing).
2. 'npm run build'(gives an optimized version which runs fast) and after build is compiled then 'npm start' (to run project).


for server - 1. 'npm run dev' (i used this for localhost and editing).
2. 'npm run build'(gives an optimized version which runs fast) and after build is compiled then 'npm start' (to run project).
